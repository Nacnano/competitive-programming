#include<bitss/stdc++.h>
using namespace std;

int pos
int main(){

    int n,k,p;
    scanf("%d%d%d",&n,&k,&p);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        pos[i]=a[i];
    }
    sort(a,a+n);

    int l=0,r=1e9;
    while(l<r)
    {
        int mid=(l+r)/2;

    }

}
